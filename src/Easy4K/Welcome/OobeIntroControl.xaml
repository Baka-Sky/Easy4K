<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Easy4K.Welcome.OobeIntroControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Opacity="0"
    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <UserControl.RenderTransform>
        <ScaleTransform x:Name="RootScale" ScaleX="1.25" ScaleY="1.25"/>
    </UserControl.RenderTransform>

    <!-- 100% 复刻 ClassIsland OobeIntroAnimationControl：
         两个独立 StackPanel 叠在同一坐标空间（方块行在上、文字行在下），方块 32x32、间距 4。 -->
    <Grid>
        <StackPanel x:Name="Blocks" Orientation="Horizontal"
                    VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="4"/>
        <StackPanel x:Name="Texts" Orientation="Horizontal"
                    VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="4"/>
    </Grid>
</UserControl>